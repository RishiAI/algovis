from AlgoVisualizer import run_visualizer

# Example array
my_array = [5, 2, 8, 1, 9, 3, 7, 4, 6]

# Run the visualizer with the example array
run_visualizer(my_array)

# In the GUI:
# 1. You'll see your initial array visualized
# 2. You can use the Control Panel to manually modify the array
# 3. Switch to the Loop Visualizer tab and enter code like:
#    for i in range(len(arr)):
#        if arr[i] % 2 == 0:
#            arr[i] = arr[i] * 2
# 4. Click "Run Visualization" to see how your code affects the array